import styled from "styled-components"

export const Card = styled.div`
    border-width: 20px;
    border-style: double;
    border-color: #030;
    opacity: 0.8;
    margin-top:250px;
    font-size:40px;

    display:flex;
    justify-content: center;
    `

export const Header = styled.div`
    width:100%;
    background: no-repeat url(fundo.jpg);

    display:flex;
    flex-direction: column;
    justify-content: center;
    `